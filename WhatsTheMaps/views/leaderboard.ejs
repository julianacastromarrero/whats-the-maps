<%# Renders leaderboard page with best-score and all-time-points rankings %>
<!DOCTYPE html>
<html>
<head>
  <title>Leaderboard</title>
  <link rel="stylesheet" href="/stylesheets/style.css">
</head>
<body>
  <%# Shared site chrome appears on every page %>
  <%- include('./partials/banner.ejs') %>

  <%- include('./partials/nav.ejs') %>

  <main class="page-shell page-shell--centered">
    <div class="section-stack">
      <%# First table ranks players by best single saved game %>
      <section class="page-card table-card">
        <h1 class="page-title">Best Score</h1>

        <% if (bestScores.length === 0) { %>
          <p class="page-subtitle">No scores have been saved yet.</p>
        <% } else { %>
          <table class="data-table">
            <thead>
              <tr>
                <th>Rank</th>
                <th>Username</th>
                <th>High Score</th>
              </tr>
            </thead>
            <tbody>
              <% bestScores.forEach((entry, index) => { %>
                <tr>
                  <td><%= index + 1 %></td>
                  <td>
                    <a
                      class="username"
                      href="<%= Number(entry.user_id) === Number(locals.user?.id) ? '/dashboard' : `/players/${entry.user_id}` %>"
                    >
                      <%= entry.username %>
                    </a>
                  </td>
                  <td><%= entry.high_score %></td>
                </tr>
              <% }) %>
            </tbody>
          </table>
        <% } %>
      </section>

      <%# Second table ranks players by cumulative points across all saved games %>
      <section class="page-card table-card">
        <h2 class="page-title">Total Points</h2>

        <% if (allTimePoints.length === 0) { %>
          <p class="page-subtitle">No scores have been saved yet.</p>
        <% } else { %>
          <table class="data-table">
            <thead>
              <tr>
                <th>Rank</th>
                <th>Username</th>
                <th>Total Points</th>
              </tr>
            </thead>
            <tbody>
              <% allTimePoints.forEach((entry, index) => { %>
                <tr>
                  <td><%= index + 1 %></td>
                  <td>
                    <a
                      class="username"
                      href="<%= Number(entry.user_id) === Number(locals.user?.id) ? '/dashboard' : `/players/${entry.user_id}` %>"
                    >
                      <%= entry.username %>
                    </a>
                  </td>
                  <td><%= entry.total_points %></td>
                </tr>
              <% }) %>
            </tbody>
          </table>
        <% } %>
      </section>
    </div>
  </main>

</body>
</html>
